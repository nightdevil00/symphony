# Assign apps
# $terminal = uwsm app -- $TERMINAL
$terminal = kitty
$browser = $HOME/.config/hypr/scripts/launch-browser.sh
$browser2 = zen-browser
$webapp = ~/.config/hypr/scripts/launch-webapp.sh
$focus = ~/.config/hypr/scripts/focus.sh
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"

# Assign Dir
$rofiDir = ~/.config/rofi/scripts
$scrDir = ~/.config/hypr/scripts/


#for default chromium (using before omarchy-chromium)
# $browser = uwsm app -- chromium --new-window --ozone-platform=wayland
# $webapp = $browser --app

# Apps
bindd = SUPER, return, Terminal, exec, $terminal
bindd = SUPER, B, Chromium, exec,  $browser
bindd = SUPER, Z, zen-browser, exec, $browser2
bindd = SUPER, E, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER, M, Music, exec, $focus spotify-launcher
bindd = SUPER, D, Discord, exec, uwsm app --  vesktop
# bindd = SUPER SHIFT, O, typora, exec, uwsm app -- typora --disable-gpu --enable-wayland-ime
bindd = SUPER, O, obsidian, exec, obsidian # --disable-gpu
bindd = SUPER, slash, Passwords, exec, uwsm app -- bitwarden-desktop
bindd = SUPER, C, code, exec, uwsm app -- code
bindd = SUPER SHIFT, D, Docker, exec, $terminal -e lazydocker
bindd = ALT, slash, Activity, exec, $terminal -e btop
bindd = ALT, M, Music, exec, $terminal -e rmpc
bindd = ALT, Q, yazi, exec, $terminal -e yazi
bindd = SUPER ALT, T, Aether, exec, $focus aether

# mpc
bindd = SUPER, F9, mpc pause, exec , mpc pause
bindd = SUPER, F10, mpc prev, exec , mpc prev
bindd = SUPER, F11, mpc play, exec , mpc play
bindd = SUPER, F12, mpc next, exec , mpc next

# Window tiling
bind = Alt, return, fullscreen

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"

# Tmux
bind = SUPER SHIFT,return, exec, $terminal -e tmux a              #attach existing session
bind = SUPER, N, exec, $terminal -e tmux new -As Practice 'nvim'  #open nvim in tmux session
bind = SUPER ALT, return, exec, $terminal -e tmux new -As main    #open new tmux-session

# Wifi menu
bindd = SUPER, I, Wifi Menu ,exec, $rofiDir/wifi.sh

# Bluetooth menu
# bindd = SUPER SHIFT, L, Bluetooth Menu, exec, blueberry
# bindd = SUPER SHIFT, L, Bluetooth Menu, exec, kitty -e $scrDir/webapp-install

bind = SUPER SHIFT, L, exec, kitty --title webapp-install -e $scrDir/webapp-install.sh

# windowrule = float, title:^(webapp-install)$

# Sound studio
# bindd = SUPER, S, Sound Studio, exec,  kitty --class=Wiremix -e wiremix 


# Close Windows
bind = SUPER, Q, killactive,

# Control tiling

# Rofi
bindd = ALT, comma, clipboard, exec, $rofiDir/clipboard.sh
bindd = SUPER ALT, M, rofibeats, exec, $rofiDir/rofibeats.sh
bindd = ALT, period, emoji, exec, $rofiDir/emoji.sh
bindd = ALT, SPACE, Find, exec, $rofiDir/rofisearch.sh
bindd = SUPER CTRL, SPACE, Pywall Themes, exec, $rofiDir/selectWall.sh
bindd = SUPER ALT, SPACE, Wallpaper Picker, exec, $rofiDir/wallPicker.sh
bindd = SUPER ALT, h, run-scripts,exec, $rofiDir/run-scripts.sh
bindd = SUPER , SPACE, app-launcher, exec, rofi -show drun
bindd = SUPER SHIFT , M, app-launcher, exec, $rofiDir/musicpicker.sh
bindd = SUPER CTRL, B, Power Profiles, exec,   $rofiDir/power-profiles.sh

bindd = SUPER SHIFT, O, Pop window out (float & pin), exec, $scrDir/pop-window

bind = SUPER SHIFT, SPACE, exec, $scrDir/toggle-waybar.sh
bind = SUPER CTRL,N , exec, $scrDir/nightlight.sh

#change theeme
bindd = SUPER CTRL, T, select swww wall, exec, $scrDir/change-theme.sh

# Power
bindd = SUPER, L, lock, exec, hyprlock
bindd = SUPER ALT, L, poweroff, exec, poweroff
bindd = SUPER, backspace , Kitty Transparency , exec, $scrDir/toggle-kitty.sh
bindd = SUPER ALT, backspace , Toggle focus & vibe mode, exec, $scrDir/toggle-focus.sh

# Screenshots
bindd = SUPER, P, Screenshot of region, exec, $scrDir/screenshot.sh
bindd = SUPER SHIFT, P, Screenshot of window, exec,  $scrDir/screenshot.sh window
bindd = SUPER CTRL, P, Screenshot of display, exec,  $scrDir/screenshot.sh output

# Screen recordings
bindd = SUPER , R, Screen record display, exec, $scrDir/record.sh
bindd = SUPER ALT, R, Screen record display with audio, exec, $scrDir/record.sh output audio

# Color picker
bindd = Alt, P, Color picker, exec, pkill hyprpicker || hyprpicker -a

# Brightness adjusts
bindd = SUPER, XF86AudioRaiseVolume, Brightness up, exec, $osdclient --brightness raise
bindd = SUPER, XF86AudioLowerVolume, Brightness up, exec, $osdclient --brightness lower

# File sharing
bindd = SUPER Alt,S, Share, exec, omarchy-menu share

#WebApps
bindd = SUPER, A, Perplexity, exec, $webapp "https://perplexity.ai"
# bindd = SUPER SHIFT,N, Netflix, exec, $webapp "https://netflix.com/in"
bindd = SUPER SHIFT, A, Grok, exec, $webapp "https://grok.com"
bindd = SUPER Alt, A, ChatGPT, exec, $webapp "https://chatgpt.com"
bindd = ALT, C, Calendar, exec, $webapp "https://calendar.google.com"
bindd = SUPER, G, Github, exec, $webapp "https://github.com/vyrx-dev"
bindd = SUPER, Y, YouTube, exec, $webapp "https://youtube.com/"
bindd = SUPER, W, WhatsApp, exec, $webapp "https://web.whatsapp.com/"
bindd = SUPER, X, X, exec, $webapp "https://x.com/"
bindd = SUPER, T, Todoist, exec, $webapp "https://app.todoist.com"
# bindd = SUPER, T, Todoist, exec, $focus_webapp "https://app.todoist.com"



# Switch audio output with Super + Mute
bindld = SUPER, XF86AudioMute, Switch audio output, exec, $scrDir/audio-switch.sh
